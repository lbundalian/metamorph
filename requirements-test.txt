# Testing dependencies for metamorph project
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
pytest-xdist>=3.0.0

# Core dependencies (if not already installed)
# Add any other dependencies your project needs here